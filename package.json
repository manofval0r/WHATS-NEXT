{
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "npm install --prefix frontend && npm run dev --prefix frontend"
  },
  "dependencies": {
    "reactflow": "^11.11.4"
  }
}